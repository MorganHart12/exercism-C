#include "luhn.h"
#include <string.h>
bool luhn(const char *num) {
    int i = 0;
    int x = 0;
    newchar[] = "";
    
    if (strlen(num) <= 1) {
        return -1;
    }
    
    while (num[i] != '\0') {
        if (i != ' ') {
            num[i] += newchar[];
        i++; 
        }
        else {
            i++;
        }
        
    }
    while (newchar[x] != '0') {
        if (x == 0 || x % 2 == 0) {
            if (newchar[x] * 2 
            
        }
        
    }
}